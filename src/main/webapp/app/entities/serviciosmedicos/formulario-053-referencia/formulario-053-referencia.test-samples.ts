import { IFormulario053Referencia, NewFormulario053Referencia } from './formulario-053-referencia.model';

export const sampleWithRequiredData: IFormulario053Referencia = {
  id: 8555,
};

export const sampleWithPartialData: IFormulario053Referencia = {
  id: 7882,
  cuadroClinico: 'offensively while',
  hallazgosRelevantes: 'finally unruly',
};

export const sampleWithFullData: IFormulario053Referencia = {
  id: 24616,
  cuadroClinico: 'hesitate soup given',
  hallazgosRelevantes: 'ugh amidst eminent',
};

export const sampleWithNewData: NewFormulario053Referencia = {
  id: null,
};

Object.freeze(sampleWithNewData);
Object.freeze(sampleWithRequiredData);
Object.freeze(sampleWithPartialData);
Object.freeze(sampleWithFullData);
